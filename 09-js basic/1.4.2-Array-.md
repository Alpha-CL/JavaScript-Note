# Array

> 数组索引以0开头。[0]是第一个数组元素，[1]是第二个数组元素，[2]是第三个...

``` javascript


// 数组子面量

var arr = [];                                       // 建议使用


// 系统定义的

var arr = new arr[];                                // 批量生产


```


``` javascript
////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////


/**
 * 稀松数组
 * 
 * 单独添加逗号不报错，也会分割为多个，返回 undefined
 */

var arr  = [,];                                     // [undefined x 1]
var arr  = [,,];                                    // [undefined x 2]


//--------------------------------------------------------------------------------------------------------------------//


// 传入多位功能相同

var arr = [1, 2, 3, , , 6];                         // arr.length = 7;

var arr = new Array(1, 2, 3, , , 6)                 // arr.length = 7;


//--------------------------------------------------------------------------------------------------------------------//


// 区别: 单独传一位功能不同

var arr = [10];                                     // [10];

var arr = new Array(10);                            // arr.length = 10;


//- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - //


/**
 * new Array 不能传入小数，会报错
 * 
 * new Array(arrayLength?: number)
 */

var arr = [10];                                     // [10.2];

var arr = new Array(10.2);                          // Invalid array length


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
```

``` javascript
////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////


/**
 * 数组中不会轻易报错
 * 
 * 一般报错原因，调用了数组没有方法
 */

var arr = [];

// 并不会报错

arr[10];                                            // undefined


// JS 中 Array 基于 Objcet

var obj = {};

obj.10;                                             // undfefined


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
```

#### 改变索引

数组元素使用其索引号进行访问

``` javascript

// 改变元素







```